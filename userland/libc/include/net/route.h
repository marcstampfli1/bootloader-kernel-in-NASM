// net/route.h - kernel routing interface (minimal). MakaOS has no routing
// socket; portable code includes this header but does not rely on its
// contents on this platform.
#ifndef _NET_ROUTE_H
#define _NET_ROUTE_H 1
#include <sys/types.h>
#include <sys/socket.h>
#endif
